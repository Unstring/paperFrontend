# Enable rewrite engine
RewriteEngine On
RewriteBase /

# Redirect to the requested resource inside the dist folder if it's not a file or directory
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /dist/$1 [L]
